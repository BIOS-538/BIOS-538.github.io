format: 
  revealjs:
    mouse-wheel: true # scroll through slides
    slide-number: true # show slide number
    hide-inactive-cursor: true # hide mouse pointer unless moved?
    reference-location: document # put references at the end
    # smaller: true # make all text smaller // use on a case-by-casis per slide or presentation
    fig-height: 4 # default for ggplot / R generated plots
    fig-width: 5 # "s
execute: 
  echo: false
editor: visual
from: markdown+emoji